{
  "name": "dtmoney-backend",
  "description": "Simples api, com json-server, para a aplicação dtmoney",
  "author": "Elvis Felix",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:server": "json-server db.json -p 3333 -w -d 500",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json-server,deploy, node, REST API, backend"
  ],
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
